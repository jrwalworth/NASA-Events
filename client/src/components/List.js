import React from 'react';

const List = () => {
return (
    <div>
        {/* {eventsData?.map((event, index) => (
            <div key={index}>
                <p>{event.id}</p>
                <p>{event.title}</p>
            </div>
         ))} */}
    </div>
);
};

export default List;